import React from "react";

export default function EmptyList(){
    return(
        <div>
            <h2>There are no Books Saved to display! Enter a search find a book to read!</h2>
        </div>
    )
}